# Unit 02: Locators and Actions

## Goal

Teach students how to find elements and perform basic interactions.

## What To Teach

- `getByRole()`
- `getByLabel()`
- `getByPlaceholder()`
- `getByText()`
- `click()`
- `fill()`
- `check()`
- `selectOption()`

## Files In This Unit

- `example.spec.js`
- `exercise.spec.js`

## After Note

Run this unit in headed mode:

```bash
npx playwright test units/02-locators-and-actions --headed
```

Run this unit in debug mode:

```bash
npx playwright test units/02-locators-and-actions --debug
```

Run a single file in debug mode:

```bash
npx playwright test units/02-locators-and-actions/example.spec.js --debug
```
