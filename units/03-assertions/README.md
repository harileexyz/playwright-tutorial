# Unit 03: Assertions

## Goal

Teach students how to validate positive and negative outcomes.

## What To Teach

- assertion purpose
- positive validation
- negative validation
- `toBeVisible()`
- `toHaveText()`
- `toHaveURL()`

## Files In This Unit

- `example.spec.js`
- `exercise.spec.js`

## After Note

Run this unit in headed mode:

```bash
npx playwright test units/03-assertions --headed
```

Run this unit in debug mode:

```bash
npx playwright test units/03-assertions --debug
```

Run a single file in debug mode:

```bash
npx playwright test units/03-assertions/example.spec.js --debug
```
