# Unit 05: Framework Basics

## Goal

Move from a direct test to a page-object-based test.

## What To Teach

- what should stay in tests
- what should move to a page object
- why reuse matters
- how a minimal framework starts

## Files In This Unit

- `pages/LoginPage.js`
- `example.spec.js`
- `exercise.spec.js`

## After Note

Run this unit in headed mode:

```bash
npx playwright test units/05-framework-basics --headed
```

Run this unit in debug mode:

```bash
npx playwright test units/05-framework-basics --debug
```

Run a single file in debug mode:

```bash
npx playwright test units/05-framework-basics/example.spec.js --debug
```
